# Run mysql -u root < initialize_test.mysql, where root is the username.

CREATE DATABASE IF NOT EXISTS ci_test;
USE ci_test;
CREATE TABLE IF NOT EXISTS ci (
  id VARCHAR(64) NOT NULL,
  status VARCHAR(7) NOT NULL,
  detail LONGTEXT,
  PRIMARY KEY (id)
)
