# Run mysql -u root < initialize.mysql, where root is the username.

CREATE DATABASE IF NOT EXISTS ci;
USE ci;
CREATE TABLE IF NOT EXISTS ci (
  id VARCHAR(64) NOT NULL,
  status VARCHAR(7) NOT NULL,
  detail LONGTEXT,
  PRIMARY KEY (id)
)
